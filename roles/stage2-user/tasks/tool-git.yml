---
- name: 'git : configure'
  template:
    src: 'gitconfig'
    dest: '{{ ansible_env.HOME }}/.gitconfig'
  tags:
    - dotfiles

- name: 'git : install hooks'
  copy:
    src: 'config/git/'
    dest: '{{ ansible_env.HOME }}/.config/git/'
    mode: '755'
