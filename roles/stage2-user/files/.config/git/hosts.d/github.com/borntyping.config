[user]
name = Sam Clements
email = sam@borntyping.co.uk

[core]
% If this isn't working, you probably used the HTTPS remote instead of the SSH remote.
sshCommand = ssh -i '~/.ssh/keys/id_ed25519_github_borntyping'
