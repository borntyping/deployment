#!/bin/bash
export ANSIBLE_REPO="$(readlink -f $(dirname $(dirname "${BASH_SOURCE[0]}")))"
export ANSIBLE_CONFIG="${ANSIBLE_REPO}/ansible.cfg"
export ANSIBLE_INVENTORY="${ANSIBLE_REPO}/inventory.conf"
exec ansible-playbook --diff "$@" "${ANSIBLE_REPO}/playbook.yml" "$@"
